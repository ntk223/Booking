global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'booking_backend'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['host.docker.internal:5000'] # Assuming backend runs on host port 5000
