/test request
read query -> require less time (200 user - 5s - 5 loop ) -> slowdown
write query -> require more time
rm -rf report result.jtl && mkdir report && jmeter -n -t test.jmx -l result.jtl -e -o report


before timeresponse ~ 700ms ( mean ) ( 200 user - 5s - 3 loop ) getAllRooms
after -> using redis cache and pagination

run docker redis
docker-compose -f cache-compose.yml up -d


cau hinh ratelimter cho auth route va room route != nhau
auth route check ip
room route check token session