/test request
read query -> require less time (200 user - 5s - 5 loop ) -> slowdown
write query -> require more time
rm -rf report result.jtl && mkdir report && jmeter -n -t test.jmx -l result.jtl -e -o report


timeresponse ~ 700ms ( mean ) ( 200 user - 5s - 3 loop )

run docker redis
docker-compose -f cache-compose.yml up -d
